<Window x:Class="HousingManagement.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="250" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0 0 0 10">
            <TextBlock Text="Логин:"/>
            <TextBox x:Name="LoginTextBox" Margin="0 5"/>
            <TextBlock Text="Пароль:"/>
            <PasswordBox x:Name="PasswordBox" Margin="0 5"/>
        </StackPanel>

        <Button Grid.Row="1" Content="Войти" Click="LoginButton_Click" Width="100" HorizontalAlignment="Center" />
    </Grid>
</Window>
